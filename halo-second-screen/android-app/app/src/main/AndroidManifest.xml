<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="Halo Second Screen"
        android:theme="@style/Theme.HaloSecondScreen"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic is needed because the PC mock/real server
          uses plain ws:// on your local network (not wss://). Fine for a
          LAN-only companion app; do not ship this to the Play Store as-is.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HaloSecondScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
